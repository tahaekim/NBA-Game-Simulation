package player;

public class PowerForward extends Player {
	public PowerForward(String name, double points, double totalRebounds, double assist, double blocks, double steals, int score) {
		super(name, "PF", points, totalRebounds, assist, blocks, steals, score);
	}
}
